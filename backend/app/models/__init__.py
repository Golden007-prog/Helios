"""Pydantic schemas — request and response shapes for every endpoint.

These are the source of truth; the TypeScript types in ``shared/typescript`` are
generated from them via ``shared/scripts/typegen.py``.
"""
