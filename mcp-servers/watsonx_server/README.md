# helios-watsonx — Custom MCP Server (Phase 1.4)

**Status: stub.** Bob owns the implementation per docs/MCP_SETUP.md and the
prompt templates in docs/CORPUS.md.

Wraps Granite Code calls with shop-aware prompt templates. Two tools:

| Tool                          | Status |
|-------------------------------|--------|
| `granite_summarize_paragraph` | stub   |
| `granite_explain_field`       | stub   |

Banned-model guard from docs/AGENTS.md must be honored. Activation: Phase
1.4 (ABEND Archaeologist).
